import React from "react";

class DoneButton extends React.Component {

    render(): React.ReactNode {
        return(
            <button className="button">Выполнено</button>
        )
    }
}

export default DoneButton;